{
  "name": "@emi-harness/tool-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Persistent policy and isolated execution boundary for EMI Harness tools",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build"
  }
}
