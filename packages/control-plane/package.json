{
  "name": "@emi-harness/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authoritative task, approval, run, and recovery state for EMI Harness",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build"
  }
}
